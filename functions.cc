#include <iostream>
#include <string>
double calculateTip (double bill, int percent) {
  // TODO: Take in the bill total using a double, and a percentage as an int
  // Calculate the total
}

void printReceipt (double subtotal, double tip) {
  // TODO: Get the terminal to print out the following:
  // Your sub-total: (Total bill before tip here)
  // Your tip: (Tip in dollars and cents)
  // Total bill: (Add them together)
}
